[project]
name = "hl-liquidity-console"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "websockets>=13",
    "pyyaml>=6",
    "polars>=1",
    "pyarrow>=17",
]

[tool.ruff]
line-length = 100
